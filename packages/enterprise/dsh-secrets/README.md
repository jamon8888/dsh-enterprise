# @deepseek-ai/dsh-enterprise-secrets

DSH Enterprise secrets — Vault/1Password injection for gateway + model-registry.

## Status

Stub implementation: `InMemoryProvider` + `process.env` fallback only.

Full README when real provider integrated.
